import * as React from 'react';
import { Component } from 'react-simplified';

export class ArticleCard extends Component <{title: string, picture: string, text: string, kat: string, importance: number}> {
    render() {
        return (
            <div>

            </div>
        );
    }
}